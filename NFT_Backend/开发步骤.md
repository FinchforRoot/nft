首先是常用命令：

初始化项目
go mod init 项目名称

执行测试用例
go test 目录 -run 具体的测试函数名 -v

运行某个go文件
go run 文件名.go

1.设计四个表

```
sync_checkpoints
- chain_id, contract_address                 唯一
- next_block, last_block_hash, updated_at

chain_events
- chain_id, contract_address, tx_hash, log_index   唯一
- block_number, block_hash, event_name
- topic0, raw_data, decoded_json, processed_at

auctions
- chain_id, auction_contract, auction_id      唯一
- seller, nft_contract, token_id
- start_price_usd_raw, start_time, end_time
- chain_status: pending/ongoing/ended/no_bid/cancelled
- highest_bid_usd_raw, highest_bidder
- highest_bid_amount_raw, bid_token_address
- created_tx_hash, ended_tx_hash, created_at, updated_at

auction_bids
- chain_event_id                              唯一
- auction_id, bidder
- bid_usd_raw, amount_raw, token_address
- tx_hash, log_index, block_number, block_time
```

2.进行model的编写
每个表对应一个结构体，单独写一个文件，文件名以小写字母为规范，这些文件使用同一个package

3.编写临时测试文件，与model同目录。
```
func NewTestDB(t *testing.T) *gorm.DB {
	t.Helper()
	db, err := gorm.Open(sqlite.Open(":memory:"), &gorm.Config{Logger: logger.Default.LogMode(logger.Info)})
	if err != nil {
		log.Fatalf("open test database: %v", err)
	}

	return db
}
```

4.在这个文件中不能引入model层的对象，因为要在model中为每个结构体生成对应的测试文件，进行测试db是否能通过测试。

5.开始编写测试函数，以chain_sync_checkpoint_test.go为例，测试文件名的结尾必须是_test，然后里面可以写多个函数，分别验证不同的场景。
根据结构体创建表的代码格式如下：
```
if err := db.AutoMigrate(&ChainSyncCheckpoint{}); err != nil {
		t.Fatalf("auto migrate checkpoint: %v", err)
	}
```
注意if的语法，中间是分号;

6.编写测试数据，在测试函数中，创建测试数据，然后进行测试。
这里遇到了很多需要习惯的代码写法。
例如查找的时候使用db.Where("chain_id = ?", 1).Find(&ChainSyncCheckpoint{})。或者db.Where("chain_id = ?", 1).First(&ChainSyncCheckpoint{}),需要提前声明用来接收查询结构的变量。 
如果是多个就是 var checkpoints []ChainSyncCheckpoint，然后db.Where("chain_id = ?", 1).Find(&checkpoints)
通常这些方法后面都需要加个.Error

VSCODE中打开关闭终端的快捷键都是`Ctrl+J`

7.验证通过后开始写配置文件
首先创建一个.env文件，这个文件千万不能上传到GitHub中，里面可以写一些环境变量，例如数据库的连接信息，或者一些配置信息。

然后安装go需要的依赖
```
go get gorm.io/driver/mysql
go get github.com/joho/godotenv
```
mysql 驱动：让 GORM 能连 MySQL
godotenv：读 .env 文件

8.开始写配置数据库的基础文件
配置必要的参数，然后创建一个config目录，里面写个database.go文件，直接根据godotenv.Load() // load .env file将配置文件中的参数加载到环境变量中。
第二步就是开始拼接数据库连接字符串，然后使用gorm.Open()打开数据库连接。
第三步检查连接是否成功，然后再设置固定的参数，例如MaxIdleConns, MaxOpenConns, Logger。


9：链上同步
① RPC 连接	        连 Sepolia 节点（用 go-ethereum），能读区块号、读日志
② ABI 解码	        把原始日志解码成事件字段
③ eth_getLogs 轮询	按区块范围批量拉代理合约的日志
④ reorg 处理        区块哈希对不上时回退重放

这一步需要安装go-ethereum依赖
go get github.com/ethereum/go-ethereum
然后.env需要添加rpc_url
SEPOLIA_RPC_URL=https://某地址

然后开始编写RPC相关的文件代码，核心是获取RPCClient

因为chain的rpc.go是个工具类，因此只要是涉及配置参数的就放在函数的入参里面，这样其他地方去调用的话就可以动态传参。
```
func NewRPCClient(rpcURL string) (*ethclient.Client, error) {
	client, err := ethclient.Dial(rpcURL)
	if err != nil {
		return nil, fmt.Errorf("connect rpc: %w", err)
	}
	return client, nil
}
```

获取到client后就可以开始获取区块以及对应合约的日志数据了
因此下一步就是需要创建一个获取链上日志的函数，这里有个坑点，不能只传个起始和终止的区块，要分块，因为每个不同的RPC节点允许你一次性拉取的区块数量是有限的，因此需要分块拉取。具体函数如下：
```
func FetchAuctionLogs(client *ethclient.Client, contractAddress string, fromBlock, toBlock, batchSize int64) ([]types.Log, error) {
	addr := common.HexToAddress(contractAddress)
	var all []types.Log
	for start := fromBlock; start <= toBlock; start += batchSize {
		end := start + batchSize - 1
		if end > toBlock {
			end = toBlock
		}
		logs, err := client.FilterLogs(context.Background(), ethereum.FilterQuery{
			Addresses: []common.Address{addr},
			FromBlock: big.NewInt(start),
			ToBlock:   big.NewInt(end),
		})
		if err != nil {
			return nil, fmt.Errorf("fetch [%d,%d]: %w", start, end, err)
		}
		all = append(all, logs...)
	}
	return all, nil
}
```
接下来就需要用到ABI了，因为项目是foundry，因此需要编译下项目拿到abi文件，然后使用go-ethereum提供的包进行解码
先进行编译操作：forge build [如果编译失败的话需要先安装对应的依赖，这里如果使用代理，可能需要配置git的代理端口，然后开启全局再进行下载，如果出现ssl的报错，多试几次]
编译之后就会在out目录下输出abi文件，格式类似***.json
此时的文件内容不能使用，需要提取出abi这个key对应的值，然后放在json文件中[特别注意格式应该是[{}]，而不是{"abi":[{}]]，最后使用go:embed的方式进行引入
```
package chain

import (
	_ "embed"
	"fmt"
	"strings"

	"github.com/ethereum/go-ethereum/accounts/abi"
)

//go:embed abi/NftAuction.json
var nftAuctionABIJSON []byte

func MustLoadNftAuctionABI() abi.ABI {
	parsed, err := abi.JSON(strings.NewReader(string(nftAuctionABIJSON)))
	if err != nil {
		panic(fmt.Sprintf("load abi: %v", err))
	}
	return parsed
}
```
然后在main.go中使用这个方法，进行日志的解码
```
parseABI := chain.MustLoadNftAuctionABI()
	for _, l := range logs {
		event, err := parseABI.EventByID(l.Topics[0])
		if err != nil || event == nil {
			fmt.Println("未知事件:", l.Topics[0].Hex())
			continue
		}
		fmt.Printf("事件=%s block=%d index=%d\n", event.Name, l.BlockNumber, l.Index)
	}
```
然后需要写一个单独的日志解析函数，入参两个，一个是ABI的解码对象，一个是日志对象；出参是日志的事件名和map组成的键值对，map[string]any{}
```
// 把一条日志解码成  事件、字段map
func DecodeLogs(parsedABI abi.ABI, l types.Log) (string, map[string]any, error) {
}
```
首先第一步是判断日志的topic长度是否为0，如果为0那么证明这个日志没有事件，那么就返回一个错误。
第二步声明日志的事件和事件对象，因为后面要从parsedABI.Events()中获取事件对象，因此这里需要声明一个变量，用于存储事件对象。
第三步获取事件对象：
```
sigHash := crypto.Keccak256Hash([]byte(ev.Sig))
		if sigHash == l.Topics[0] {
			eventName = name
			event = ev
			break
		}
```
还有一种方法是使用abi.EventByID()方法
```
 event, err := parsedABI.EventByID(l.Topics[0])
    if err != nil || event == nil {
        return "", nil, fmt.Errorf("unknown event: %s", l.Topics[0].Hex())
    }
```
获取到事件名称和事件后，就是对事件进行解析了。
先声明一个map[string]any{}，用于存储事件参数。然后开始解析【这里要针对indexed参数和non-indexed参数分别进行解析】：
至此，解析日志的功能就写完了，接下来开始将链上的事件日志存储到数据库中

准备给数据库中存储解析的数据，所以得先在链上跑个业务流程
```
首先需要设置环境变量
export NFT=
export USDT=
export PROXY=
export ACCOUNT1=
export SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/<your_infura_id>

0.准备NFT合约给某个账户先生成一个NFT
cast send $NFT "mint(address)" 账户地址 --account deployer1 --rpc-url $SEPOLIA_RPC_URL

1.先把自己创建的USD合约  mint一些钱给到要参与竞拍的人account1
cast send $USDT "mint(address,uint256)" $ACCOUNT1 1000000000 --account deployer1 --rpc-url $SEPOLIA_RPC_URL

2.将自己的NFT合约授权给交易所代理合约，这样交易所代理合约才能操作NFT
cast send $NFT "setApprovalForAll(address,bool)" $PROXY true --account deployer1 --rpc-url $SEPOLIA_RPC_URL

2.1 想要查看NFT的tokenID，直接去https://sepolia.etherscan.io/类似的网站搜索NFT合约地址，或者搜索

3.创建一个拍卖
cast send $PROXY "createAuction(address,uint256,uint256,uint256,uint256)" $NFT 0 100 0 12 --account deployer1 --rpc-url $SEPOLIA_RPC_URL

4.account1授权交易所代理合约可以动用自己的USD
cast send $USDT "approve(address,uint256)" $PROXY 1000000000 --account account2 --rpc-url $SEPOLIA_RPC_URL

5.account1 用 USDT 出价（101 USDT）
cast send $PROXY "placeBid(uint256,uint256,address)" 0 101000000 $USDT --account account2 --rpc-url $SEPOLIA_RPC_URL

6.（等过了 12 小时）结束拍卖
cast send $PROXY "endAuction(uint256)" 0 --account deployer1 --rpc-url $SEPOLIA_RPC_URL
```
跑完业务之后进行main的流程组装
1.初始化数据库、RPC客户端等
2.开启一个协程，专门定时抓取链上的日志，然后存储到本地的mysql数据库中
3.开启http服务，监听特定端口，提供前端API查询服务