1.parsedABI.Events返回的是什么？代码怎么遍历？这个方法是为了干什么
因为是map，所以遍历的时候第一个参数是key，第二个参数是value。 key可以理解为合约中的方法名字，例如func foo() 那么key就是foo
```text
type Event struct {
	// Name 是事件在内部表示中使用的名称。它由原始名称派生而来，
	// 在事件重载的情况下会添加数字后缀（如 foo, foo0, foo1...）。
	// 
	// 例如：
	// 以下两个事件具有相同的名称：
	// * foo(int,int)
	// * foo(uint,uint)
	// 第一个事件的内部名称将解析为 foo，而第二个事件
	// 将被解析为 foo0。
	Name string

	// RawName 是从 ABI JSON 中解析出的原始事件名称，未经任何修改。
	// 保留用于调试和与原始定义对照。
	RawName string

	// Anonymous 标识该事件是否为匿名事件。
	// 匿名事件不会将事件签名作为日志主题进行索引，
	// 因此无法通过事件名称进行过滤查询，但可以节省 Gas 费用。
	Anonymous bool

	// Inputs 是事件的输入参数列表，包含每个参数的名称、类型
	// 以及是否为索引参数（Indexed）等信息。
	// 用于编码/解码事件日志中的数据部分和索引主题。
	Inputs Arguments

	// str 是内部缓存的字符串表示（私有字段）。
	// 用于缓存 String() 方法的返回结果，避免重复构造签名字符串。
	str string

	// Sig 是符合 ABI 规范的事件签名字符串。
	// 格式为 "事件名(参数类型列表)"，参数类型使用规范类型名。
	// 例如：event foo(uint32 a, int b) 的 Sig 为 "foo(uint32,int256)"
	// 注意："int" 会被规范化为 "int256"。
	Sig string

	// ID 是事件的唯一标识符，即事件签名（Sig）的 Keccak-256 哈希值。
	// 该哈希值会作为日志的第一个主题（Topic）存储在以太坊区块链中
	// （匿名事件除外），用于在检索或过滤日志时快速定位特定事件。
	ID common.Hash
}
```
2.abi.Event里面是什么，如何理解？怎么提取对应的参数和对应的值？
```text
abi的event结构体

event中主要涉及2个需要提取的东西，一个是index的入参，一个是data入参
// 用于存储这个事件的入参和对应的值
	fields := map[string]any{}
// 解析indexed参数
	indexIdx := 0
	for _, arg := range event.Inputs {
		if !arg.Indexed {
			continue
		}
		topicPos := 1 + indexIdx
		indexIdx++
		// 如果topic位置超出范围，让程序继续运行不要报错
		if topicPos > len(l.Topics) {
			continue
		}
		topic := l.Topics[topicPos]
switch arg.Type.T {
		case abi.AddressTy:
			fields[arg.Name] = common.BytesToAddress(topic.Bytes())
		case abi.IntTy, abi.UintTy:
			fields[arg.Name] = new(big.Int).SetBytes(topic.Bytes())
		case abi.BoolTy:
			fields[arg.Name] = topic[31] != 0
		default:
			fields[arg.Name] = topic.Hex()
		}
// 解析data入参
if len(l.Data) > 0 {
		values, err := parsedABI.Unpack(eventName, l.Data)
		if err != nil {
			return eventName, fields, fmt.Errorf("unpack data: %w", err)
		}
		nonIdx := 0
		for _, arg := range event.Inputs {
			if arg.Indexed {
				continue
			}
			if nonIdx < len(values) {
				fields[arg.Name] = values[nonIdx]
				nonIdx++
			}
		}
	}
```

3.遇到事件中参数是bool类型的时候怎么解码？
```
case abi.BoolTy:
	fields[arg.Name] = topic[31] != 0
```

4.解析事件日志的时候针对indexed和data应该具体解析什么字段？
indexed需要解析type.Log的Topics字段，data需要解析type.Log的Data字段

5.任何类型转json字符串的方法
```
jsonBytes, err := json.Marshal(fields)
```

6.如何使用cast【foundry框架】去调用链上的合约的某个方法？
```
cast call $CONTRACT_ADDRESS "number()" --rpc-url $RPC_URL
cast call $CONTRACT_ADDRESS "balanceOf(address)" $ADDRESS --rpc-url $RPC_URL
```

7.如何使用foundry升级合约？
```
在完成这个作业的时候，遇到个问题，有个事件缺少了参数，导致关键信息无法保存到mysql中，因此需要重新升级合约。
但因为事件是在内部emit的，因此不能通过继承来更改，只能通过复制后修改的方式。

所以升级合约的步骤就改为了复制原来的合约，然后修改里面的事件。
具体步骤如下
1. 完整复制旧合约代码
2. 改合约名为 `NftAuctionV3`
3. 修改事件定义（加参数）
4. 修改 `emit` 语句（传新参数）
5. 确保 storage 布局不变
6. 部署新逻辑合约，通过 UUPS 升级


forge build

# 部署新实现（实现合约不要 initialize）
forge create src/NftAuctionV3.sol:NftAuctionV3 --rpc-url $RPC --private-key $PK
forge create src/NftAuctionV3.sol:NftAuctionV3 --rpc-url $SEPOLIA_RPC_URL --account deployer1 

上面的命令是模拟部署，不会消耗gas
如果要真实部署的话  那么就是
forge create src/NftAuctionV3.sol:NftAuctionV3 --rpc-url $SEPOLIA_RPC_URL --account deployer1 --broadcast

# 用 admin 账户通过代理升级
cast send 0xA11b8629fC9d16F6DdEa8fBa3921B27208160A26 \
  "upgradeTo(address)" <V3实现地址> --rpc-url $RPC --private-key $PK

# 验证：读 ERC1967 implementation slot，应指向 V3
cast storage 0xA11b8629fC9d16F6DdEa8fBa3921B27208160A26 \
  0x360894a13ba1a3210667c828492db98dca3e2076cc3735a920a3ca505d382bbc --rpc-url $RPC

# 重新生成后端 ABI
jq '.abi' out/NftAuctionV3.sol/NftAuctionV3.json > ../nft_backend/chain/abi/NftAuction.json

其中$RPC需要在当前黑窗口创建好，这个变量默认只在当前会话有效
export RPC=https://sepolia.infura.io/v3/<your_infura_id>
查看的时候是 echo $RPC
如果自己配置过了cast wallet import deployer1 --interactive 那么就不需要用--private-key $PK  而是 --account deployer1

```

8.如何从map中获取对应的值？
```
假设原始的数据struct为
event NewHighestBid(
        uint256 indexed auctionId,  // 拍卖品id
        address indexed bidder, // 拍卖者地址
        uint256 bid,    // 出价金额【美元】
        uint256 bidAmount, // 代币数量【ETH/ERC20】,
        address tokenAddress
    );
address类型的话：bidder, ok := decoded["bidder"].(common.Address)
uint256类型的话：bid, ok := decoded["bid"].(*big.Int)

```
9.map中是uint256类型，但是数据库中存储用的是string，应该怎么处理？

10.如何看自己都有哪些NFT，并且这些NFT的tokenId是多少？
去链上查询，可以查询特定的交易hash或者对应的账户地址或者对应的NFT合约

11.需求：第一次查询数据库中时，如果没有记录就新增，否则就更新。应该怎么判断？
参考
```
checkpoint, err := repository.FindCheckpoint(db, chainId, proxy_address)
	if err != nil && !errors.Is(err, gorm.ErrRecordNotFound) {
		panic(err)
	}
```
12.go语言中，切片的新增代码怎么写？
```
var all []types.Log
all = append(all, logs...)
```

13.go语言中，判断某个error是不是特定的某个类型
```
if err != nil && !errors.Is(err, gorm.ErrRecordNotFound) {
		panic(err)
	}
```

14.如何获取特定区块号的区块头
```
header, err := client.HeaderByNumber(context.Background(), big.NewInt(int64(l.BlockNumber)))

获取区块头的出块时间
bt = time.Unix(int64(header.Time), 0).UTC()
```

15.go语言中如何立刻执行一个go 协程？
```
首先了解函数定义
func go(){}
然后了解go协程的语法
go func(){}()
后面加括号表示立即执行，关于入参也可以通过后面的括号进行传递
go func(a int, b int) int{return a+b}(1,2)  表示立即执行这个函数，入参为1和2 返回值为3
```
16.在gorm中  想把某些表的数据全部删除，代码怎么写？
```
tx 即 *gorm.DB
if err := tx.Where("1=1").Delete(&model.AuctionBid{}).Error; err != nil {
					return err
				}

在开发过程中，针对某些操作要将其包裹在事务中，具体代码
if err := db.Transaction(func(tx *gorm.DB) error {
  // DB操作
}
```